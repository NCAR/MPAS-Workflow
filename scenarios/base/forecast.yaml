forecast:
## updateSea
# whether to update surface fields before a forecast (e.g., sst, xice)
# OPTIONS: True/False
  updateSea: True

## mesh-specific settings
  # resource requirements
  job:
    #{{outerMesh}}:
    #  baseSeconds: int
    #  secondsPerForecastHR: int
    #  nodes: int
    #  PEPerNode: int
    30km:
      # faster turnaround
      nodes: 16
      PEPerNode: 32
      baseSeconds: 60
      secondsPerForecastHR: 120
      # more efficient
      #nodes: 4
      #PEPerNode: 36
      #baseSeconds: 60
      #secondsPerForecastHR: 220
    60km:
      nodes: 4
      PEPerNode: 36
      baseSeconds: 60
      secondsPerForecastHR: 120
    120km:
      nodes: 4
      PEPerNode: 32
      baseSeconds: 60
      secondsPerForecastHR: 60
