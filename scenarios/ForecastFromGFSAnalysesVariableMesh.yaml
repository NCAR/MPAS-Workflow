suite: ForecastFromExternalAnalyses

externalanalyses:
  resource: "GFS.PANDAC"

#build:
#  forecast directory: path-for-mpas-model-build

experiment:
  name: 'ForecastFromGFSAnalysesMPT_60-3km_coldstart'

build:
  mpas bundle: /glade/derecho/scratch/bjung/saca/mpas-bundle/build_single

extendedforecast:
  meanTimes: T00 #T00, T12
  lengthHR: 36
  outIntervalHR: 1
  execute: False # uncomment if forecasts are already completed
  # optionally turn on extended forecast post
  post: [verifyobs]

#initic:
#  job:
#    60-3km:
#      nodes: 4
#      PEPerNode: 32

forecast:
  job:
    60-3km:
      nodes: 4
      PEPerNode: 128
      baseSeconds: 60
      secondsPerForecastHR: 210

hofx:
  observers: [
    abi_g16,
  ]
  concatenateObsFeedback: True

hpc:
  CriticalQueue: economy
  NonCriticalQueue: economy

members:
  n: 1

model:
  outerMesh: 60-3km
  GraphInfoDir: /glade/campaign/mmm/parc/ivette/pandac/saca/all_graph

observations: # for optional verification
  resource: SacaObs

workflow:
  first cycle point: 20180415T00
  final cycle point: 20180415T00
  max active cycle points: 37
