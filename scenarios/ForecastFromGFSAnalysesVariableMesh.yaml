suite: ForecastFromExternalAnalyses

externalanalyses:
  resource: "GFS.PANDAC"

#build:
#  forecast directory: path-for-mpas-model-build

experiment:
  name: 'ForecastFromGFSAnalysesMPT_60-3km_coldstart_bugfix'

build:
  mpas bundle: /glade/derecho/scratch/bjung/saca/mpas-bundle.modif2_bugfix/build_single
  #/glade/derecho/scratch/bjung/saca/mpas-bundle/build_single

extendedforecast:
  meanTimes: T00 #T00, T12
  lengthHR: 36
  outIntervalHR: 1
  execute: False # uncomment if forecasts are already completed
  # optionally turn on extended forecast post
  post: [verifyobs] #verifyobs

#initic:
#  job:
#    60-3km:
#      nodes: 4
#      PEPerNode: 32

forecast:
  job:
    60-3km:
      nodes: 4
      PEPerNode: 128
      baseSeconds: 60
      secondsPerForecastHR: 240

hofx:
  observers: [
    abi_g16,
  ]
  concatenateObsFeedback: True
  IRVISlandCoeff: IGBP

hpc:
  CriticalQueue: economy
  NonCriticalQueue: economy

members:
  n: 1

model:
  outerMesh: 60-3km
  GraphInfoDir: /glade/campaign/mmm/parc/ivette/pandac/saca/all_graph

observations: # for optional verification
  resource: SacaObs

workflow:
  first cycle point: 20180415T00
  final cycle point: 20180430T00
  max active cycle points: 1
