suite: ForecastFromExternalAnalyses

externalanalyses:
  resource: "GFS.PANDAC"

experiment:
  name: 'ColdStart_clean2.1'
  prefix: ''

build:
  mpas bundle: /glade/derecho/scratch/bjung/saca/mpas-bundle.modif2_bugfix.traj_fix/build_single

extendedforecast:
  meanTimes: T00
  lengthHR: 18
  outIntervalHR: 1
  execute: False # uncomment if forecasts are already completed
  # optionally turn on extended forecast post
  post: [verifyobs] #verifyobs, verifymodel

hofx:
  observers: [
    abi_g16,
  ]
  concatenateObsFeedback: True
  IRVISlandCoeff: IGBP

hpc:
  CriticalQueue: economy
  NonCriticalQueue: economy

members:
  n: 1

model:
  outerMesh: 60-3km
  innerMesh: 60km
  GraphInfoDir: /glade/campaign/mmm/parc/ivette/pandac/all_graph

observations: # for optional verification
  resource: SacaObs

workflow:
  first cycle point: 20180424T00
  final cycle point: 20180425T00
  max active cycle points: 16
