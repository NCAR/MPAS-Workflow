verifymodel:
  # resource requirements
  job:
    # cylc retry string
    retry: '1*PT30S'

    #{{outerMesh}}:
    #  baseSeconds: int
    #  secondsPerMember: int
    defaults:
      baseSeconds: 2000
      secondsPerMember: 500
    30km:
      baseSeconds: 1200
    60km:
      baseSeconds: 480
      secondsPerMember: 100
    120km:
      baseSeconds: 120
      secondsPerMember: 40
