suite: CloudDirectInsertion

experiment:
  name: 'CloudDirectInsertion_from0hr_60-3km_bugfix'
  #'ForecastFromColdStartFC-6hr_60-3km'
  #'CloudDirectInsertion_60-3km_dependencies'
  #'CloudDirectInsertion_from0hr_60-3km'
  #'CloudDirectInsertion_60-3km_from0hr'
  #'ForecastFromColdStartFC-6hr_60-3km'
  #'CloudDirectInsertion_60-3km_modif2'
  #'ForecastFromGFSAnalysesMPT_60-3km_coldstart'

build:
  mpas bundle: /glade/derecho/scratch/bjung/saca/mpas-bundle.modif2_bugfix/build_single
  #/glade/derecho/scratch/bjung/saca/mpas-bundle/build_single
  # modif2 98% + update T and Qv
  # /glade/derecho/scratch/bjung/saca/mpas-bundle.modif2/build_single
  # modif 98%
  # /glade/derecho/scratch/bjung/saca/mpas-bundle.modif/build_single
  # default 50%
  #/glade/derecho/scratch/bjung/saca/mpas-bundle/build_single

externalanalyses:
  resource: "GFS.PANDAC"

saca:
  meanTimes: T00
  #runSaca: False #this does not run saca but copies the 6hr fcst for exp starting from 6hr
  #execute: False

extendedforecast:
  meanTimes: T00
  lengthHR: 36
  outIntervalHR: 1 #6 #1
  DACycling: True
  #execute: False # uncomment if forecasts are already completed
  post: [] #verifyobs

forecastsaca:
  #execute: False
  post: [] #verifymodel,verifyobs

hofx:
  observers: [
    abi_g16,
  ]
  concatenateObsFeedback: True
  IRVISlandCoeff: IGBP

hpc:
  CriticalQueue: economy
  NonCriticalQueue: economy

members:
  n: 1

model:
  outerMesh: 60-3km
  innerMesh: 60km
  ensembleMesh:  60km
  GraphInfoDir: /glade/campaign/mmm/parc/ivette/pandac/saca/all_graph

observations:
  resource: SacaObs

workflow:
  first cycle point: 20180415T00
  final cycle point: 20180430T00
  prevBgHR: 0 #0 #6
  max active cycle points: 16
