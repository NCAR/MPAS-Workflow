suite: CloudDirectInsertion

experiment:
  name: 'CloudDirectInsertion_clean2.1'
  prefix: ''

build:
  mpas bundle: /glade/derecho/scratch/bjung/saca/mpas-bundle.modif2_bugfix.traj_fix/build_single

externalanalyses:
  resource: "GFS.PANDAC"

firstbackground:
  resource: "PANDAC.GFS"

saca:
  meanTimes: T00
  #runSaca: False #this does not run saca but copies the 6hr fcst (the specified prevBgHR) for exp starting from 6hr
  execute: False

extendedforecast:
  meanTimes: T00
  lengthHR: 18
  outIntervalHR: 1
  DACycling: True
  updateSea: True
  execute: False # uncomment if forecasts are already completed
  post: [verifyobs] #verifyobs, verifymodel

forecastsaca:
  execute: False
  post: [] #verifymodel,verifyobs

hofx:
  observers: [
    abi_g16,
  ]
  concatenateObsFeedback: True
  IRVISlandCoeff: IGBP

hpc:
  CriticalQueue: economy
  NonCriticalQueue: economy

members:
  n: 1

model:
  outerMesh: 60-3km
  innerMesh: 60km
  ensembleMesh:  60km
  GraphInfoDir: /glade/campaign/mmm/parc/ivette/pandac/all_graph

observations:
  resource: SacaObs

workflow:
  first cycle point: 20180430T00
  final cycle point: 20180430T00
  max active cycle points: 16
