  obs filters:
  - filter: Domain Check
    filter variables:
    - name: brightness_temperature
      channels: *mhs_metop-a_channels
    where:
    - variable:
        name: land_area_fraction@GeoVaLs
      minvalue: 1.0
